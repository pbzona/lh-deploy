import SyntaxHighlighter from 'react-syntax-highlighter';
import { docco } from 'react-syntax-highlighter/dist/cjs/styles/hljs';


# Step 1: Import the LaunchDarkly SDK
For this lesson, we will be working out of the `index.js` file inside of the `module01` directory. Click the button below to open the correct file now:

<button> Open File </button>

Before we can initialize our client, we'll need to import the **LaunchDarkly SDK**.

We can accomplish this by adding the name of our SDK module, `launchdarkly-node-server-sdk`, inside of the `require()` statement on line 12 of `module01/index.js`:


<SyntaxHighlighter showLineNumbers="true" startingLineNumber={10} language="javascript" style={docco}>
{
`  // SDK Setup
  // STEP 1: Import the correct module
  const ld = require();`
}
</SyntaxHighlighter>

Make sure your module name has single quotes surrounding it, like so:


<SyntaxHighlighter showLineNumbers="true" startingLineNumber={10} language="javascript" style={docco}>
{
`  // SDK Setup
  // STEP 1: Import the correct module
  const ld = require('launchdarkly-node-server-sdk');`
}
</SyntaxHighlighter>